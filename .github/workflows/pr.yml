# ─── Pull Request Pipeline ────────────────────────────────────────────────────
# Fast checks that run on every PR. No Docker, no Azure credentials needed.
#
# Scope-aware pipeline (see docs/architecture/ci-scope-testing.md):
#   changes  — detects whether this push added non-housekeeping commits.
#              Skips everything when only chore: bump version / Merge branch
#              commits were pushed (e.g. "Update branch" housekeeping).
#   filter   — uses dorny/paths-filter to scope jobs to what actually changed.
#              Falls back to running all jobs if the filter step itself errors.
#   ci-passed — single gate job required by branch protection. Passes when
#               every job either succeeded or was legitimately skipped.
#
# Jobs:
#   1. lint-ps    — PSScriptAnalyzer on all PowerShell files
#   2. lint-js    — ESLint on the frontend (app/ui)
#   3. unit-tests — Pester tests with code coverage report
#   4. unit-js    — Vitest tests for the API (validation, middleware)
#   5. unit-ui    — Vitest tests for the UI
#   6. node-launcher-ui-build — crawler wizard bundle check
#   7. openapi    — Spectral lint on app/api/src/openapi.yaml
#   8. audit      — npm audit on frontend and API packages
#   9. hygiene    — tests + changelog fragment required for code changes
#  10. crawler-manifest — CrawlerMeta.js completeness check
#  11. docs-nav   — crawler docs registered in mkdocs.yml nav
#
# Target branches: PRs into main or dev
# ─────────────────────────────────────────────────────────────────────────────

name: PR Checks

on:
  pull_request:
    branches:
      - main
      - dev
      - 'release/**'

env:
  DOTNET_NOLOGO: true
  POWERSHELL_TELEMETRY_OPTOUT: 1

jobs:
  # ── Commit-range skip ────────────────────────────────────────────────────
  # Detects whether this push added non-housekeeping commits. If every new
  # commit is "chore: bump version" or "Merge branch 'main'", CI skips.
  # This fixes the "Update branch" re-run problem: after merging main, the
  # PR diff still shows product files but no new product commits were added.
  changes:
    name: 'Detect: product changes in this push'
    runs-on: ubuntu-latest
    outputs:
      has_product_changes: ${{ steps.check.outputs.result }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 0

      - id: check
        run: |
          # First push to a new branch: before is all-zeros. Always run CI.
          if [ "${{ github.event.before }}" = "0000000000000000000000000000000000000000" ]; then
            echo "result=true" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # After a rebase or force-push, event.before may no longer be in
          # the rewritten history. Treat this as a real change (safe default).
          if ! git merge-base --is-ancestor "${{ github.event.before }}" "${{ github.sha }}" 2>/dev/null; then
            echo "result=true" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # Count commits added by this push that are not housekeeping.
          # Use wc -l (always exits 0) instead of grep -cvE (exits 1 on empty
          # input, causing || echo 0 to fire and produce "0\n0").
          NON_HOUSEKEEPING=$(
            git log "${{ github.event.before }}..${{ github.sha }}" --format="%s" \
              | grep -v '^$' \
              | grep -vE "^chore: bump version to [0-9]|^Merge branch 'main' into " \
              | wc -l \
              | tr -d ' '
          )
          if [ "$NON_HOUSEKEEPING" -gt 0 ]; then
            echo "result=true" >> "$GITHUB_OUTPUT"
            echo "Found $NON_HOUSEKEEPING non-housekeeping commit(s) — running CI." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "result=false" >> "$GITHUB_OUTPUT"
            printf "CI skipped — no product code changes in this push.\n\nOnly housekeeping commits detected (chore: bump version / Merge branch 'main').\nIf this is wrong, push a real commit or add the \`integration-test\` label.\n" >> "$GITHUB_STEP_SUMMARY"
          fi

  # ── Path-scope filter ────────────────────────────────────────────────────
  # Scopes jobs to what actually changed. The fallback-to-run-all (outcome ==
  # 'failure') ensures a dorny/paths-filter outage doesn't silently skip jobs
  # and produce a false green.
  filter:
    name: 'Detect: changed path scopes'
    needs: [changes]
    if: needs.changes.outputs.has_product_changes == 'true'
    runs-on: ubuntu-latest
    outputs:
      ps:      ${{ steps.f.outputs.ps }}
      pester:  ${{ steps.f.outputs.pester }}
      api:     ${{ steps.f.outputs.api }}
      ui:      ${{ steps.f.outputs.ui }}
      openapi: ${{ steps.f.outputs.openapi }}
      outcome: ${{ steps.f.outcome }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3
        id: f
        with:
          filters: |
            ps:
              - 'tools/crawlers/**/*.ps1'
              - 'tools/powershell-sdk/**'
              - 'setup/**/*.ps1'
              - 'setup/IdentityAtlas.psm1'
            pester:
              - 'tools/powershell-sdk/**'
              - 'tools/crawlers/shared/**'
              - 'setup/docker/Invoke-CrawlerJob.ps1'
              - 'setup/IdentityAtlas.psm1'
              - 'test/unit/**'
            api:
              - 'app/api/src/**'
            ui:
              - 'app/ui/src/**'
              - 'tools/crawlers/**/*.jsx'
              - 'tools/crawlers/**/*.test.js'
              - 'tools/crawlers/**/*.test.jsx'
            openapi:
              - 'app/api/src/openapi.yaml'

  # ── Stage 1: PowerShell linting ──────────────────────────────────────────
  lint-ps:
    name: 'Lint: PSScriptAnalyzer'
    needs: [filter]
    if: |
      needs.filter.outputs.ps == 'true' ||
      needs.filter.result == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Install PSScriptAnalyzer
        shell: pwsh
        run: Install-Module PSScriptAnalyzer -Force -Scope CurrentUser

      - name: Analyse PowerShell files
        shell: pwsh
        run: |
          # Scan all production PowerShell roots.
          #
          # Excluded paths:
          #   test/               — test-harness scripts use non-approved verbs
          #                         (Report-Result) intentionally; not production code
          #   setup/CreatePSD.ps1 — developer-only manifest regeneration tool with
          #                         hardcoded local paths; not shipped in the container
          #
          # Excluded rules:
          #   PSAvoidUsingWriteHost — Write-Host is the intentional logging mechanism
          #                           throughout the crawler / worker codebase
          $paths = @(
            (Get-ChildItem 'tools/powershell-sdk' -Directory | Select-Object -ExpandProperty FullName)
            'tools/riskscoring',
            'tools/crawlers',
            'tools/csv-templates/transforms',
            'setup/docker',
            'setup/IdentityAtlas.psm1'
          )
          $excludeRules = @(
            'PSAvoidUsingWriteHost'                    # Write-Host is the intentional logging mechanism
            'PSAvoidGlobalVars'                        # intentional: token caching uses globals across function calls
            'PSUseShouldProcessForStateChangingFunctions'  # internal library functions, not interactive cmdlets
            'PSUseBOMForUnicodeEncodedFile'            # Linux-first codebase; BOM is unnecessary
            'PSUseSingularNouns'                       # internal naming convention; not module exports
            'PSAvoidDefaultValueSwitchParameter'       # crawler phase switches default to $true intentionally
            'PSUseProcessBlockForPipelineCommand'      # these functions are not designed for pipeline use
            'PSUseApprovedVerbs'                       # Map- prefix used for private script-level helpers
            'PSAvoidUsingEmptyCatchBlock'              # empty catches are intentional for non-critical operations
            'PSUseDeclaredVarsMoreThanAssignments'     # investigate separately; too noisy for CI gate
            'PSAvoidUsingInvokeExpression'             # scheduler crontab is image-baked, not dynamic user input
            'PSReviewUnusedParameter'                  # some parameters are kept for dispatcher API compatibility
          )
          $results = @()
          foreach ($p in $paths) {
            if (Test-Path $p) {
              $results += Invoke-ScriptAnalyzer -Path $p -Recurse `
                -Severity Warning, Error `
                -ExcludeRule $excludeRules
            }
          }
          if ($results) {
            $results | Format-Table RuleName, Severity, ScriptName, Line, Message -AutoSize
            Write-Host "::error::PSScriptAnalyzer found $($results.Count) issue(s)"
            exit 1
          }
          Write-Host "PSScriptAnalyzer: no issues found" -ForegroundColor Green

  # ── Stage 2: JavaScript linting ──────────────────────────────────────────
  lint-js:
    name: 'Lint: ESLint'
    needs: [filter]
    if: |
      needs.filter.outputs.ui == 'true' ||
      needs.filter.result == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: app/ui/package-lock.json

      - name: Install UI dependencies
        working-directory: app/ui
        run: npm ci

      - name: Run ESLint (UI)
        working-directory: app/ui
        run: npm run lint

      - name: Install API dependencies
        working-directory: app/api
        run: npm ci

      - name: Run ESLint (API — security rules)
        working-directory: app/api
        run: npm run lint

  # ── Stage 3: Pester unit tests ───────────────────────────────────────────
  unit-tests:
    name: 'Unit Tests: Pester'
    needs: [filter]
    if: |
      needs.filter.outputs.pester == 'true' ||
      needs.filter.result == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Install Pester
        shell: pwsh
        run: |
          # ubuntu-latest ships Pester pre-loaded, so a plain Install-Module -Force
          # always warns that it can't overwrite the in-use version. Skip the install
          # when the runner already satisfies the minimum version.
          if (-not (Get-Module Pester -ListAvailable | Where-Object Version -ge '5.0.0')) {
            Install-Module Pester -MinimumVersion 5.0.0 -Force -Scope CurrentUser
          }

      - name: Run Pester tests with coverage
        shell: pwsh
        run: |
          $cfg = New-PesterConfiguration
          $cfg.Run.Path = 'test/unit'
          $cfg.Run.PassThru = $true
          $cfg.Output.Verbosity = 'Detailed'
          $cfg.TestResult.Enabled = $true
          $cfg.TestResult.OutputPath = 'pester-results.xml'
          $cfg.TestResult.OutputFormat = 'JUnitXml'
          $cfg.CodeCoverage.Enabled = $true
          $cfg.CodeCoverage.Path = @(
            (Get-ChildItem 'tools/powershell-sdk' -Directory | Select-Object -ExpandProperty FullName)
            (Get-ChildItem 'tools/crawlers' -Directory |
              Where-Object { Test-Path (Join-Path $_.FullName 'crawler.json') } |
              Select-Object -ExpandProperty FullName)
            'tools/riskscoring'
          )
          $cfg.CodeCoverage.OutputPath = 'pester-coverage.xml'
          $cfg.CodeCoverage.OutputFormat = 'JaCoCo'
          $result = Invoke-Pester -Configuration $cfg
          Write-Host "Coverage: $([Math]::Round($result.CodeCoverage.CoveragePercent, 1))%"
          if ($result.FailedCount -gt 0) { exit 1 }

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: pester-results
          path: |
            pester-results.xml
            pester-coverage.xml
          retention-days: 14

  # ── Stage 4: API unit tests (Vitest) ────────────────────────────────────
  unit-js:
    name: 'Unit Tests: Vitest (API)'
    needs: [filter]
    if: |
      needs.filter.outputs.api == 'true' ||
      needs.filter.result == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: app/api/package-lock.json

      - name: Install dependencies
        working-directory: app/api
        run: npm ci

      - name: Run Vitest
        working-directory: app/api
        run: npm test

  # ── Stage 4b: UI unit tests (Vitest) ────────────────────────────────────
  unit-ui:
    name: 'Unit Tests: Vitest (UI)'
    needs: [filter]
    if: |
      needs.filter.outputs.ui == 'true' ||
      needs.filter.result == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: app/ui/package-lock.json

      - name: Install dependencies
        working-directory: app/ui
        run: npm ci

      - name: Run Vitest
        working-directory: app/ui
        run: npm test

  # ── Stage 4c: Node-launcher UI build (crawler wizard bundling) ──────────
  # The portable/desktop build (app/desktop/scripts/build-node-launcher.mjs)
  # builds the UI from a separate pipeline than Docker's. Crawler wizards
  # under tools/crawlers/ broke that build the moment the first wizard
  # (midpoint, #336) shipped — caught only by this check, not by unit-ui or
  # lint-js, because it depends on the *production build* step, not the
  # source files. It went unnoticed on main for hours because the script
  # otherwise only runs on a beta/release cut (cut-beta.yml etc.), not on
  # every PR. --ui-only runs just the UI build + its own crawler-wizard
  # bundle check, skipping the Windows-only esbuild/pglite/re2/node.exe/zip
  # steps so this has no pwsh dependency and stays fast.
  node-launcher-ui-build:
    name: 'Build: Node-launcher UI (crawler wizards)'
    needs: [filter]
    if: |
      needs.filter.outputs.ui == 'true' ||
      needs.filter.result == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: '24'

      - name: Build UI via the node-launcher pipeline
        run: node app/desktop/scripts/build-node-launcher.mjs --ui-only

  # ── Stage 5: OpenAPI spec linting ───────────────────────────────────────
  openapi:
    name: 'Lint: OpenAPI spec'
    needs: [filter]
    if: |
      needs.filter.outputs.openapi == 'true' ||
      needs.filter.result == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: '24'

      - name: Install Spectral
        run: npm install -g @stoplight/spectral-cli

      - name: Lint openapi.yaml
        run: spectral lint app/api/src/openapi.yaml

  # ── Stage 6: Dependency audit ────────────────────────────────────────────
  audit:
    name: 'Audit: npm audit'
    needs: [changes]
    if: needs.changes.outputs.has_product_changes == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: '24'

      - name: Audit frontend dependencies
        working-directory: app/ui
        run: npm audit --audit-level=high

      - name: Audit API dependencies
        working-directory: app/api
        run: npm audit --audit-level=high

  # ── Stage 7: PR hygiene (tests + changelog required for code changes) ────
  # Blocks merging a code change that ships without tests or a changelog
  # fragment — the gap that let an undocumented, untested feature reach main.
  # Escape hatch: a maintainer can apply the 'skip-hygiene' label, which
  # skips the job entirely (job-level if, so it is never even scheduled).
  hygiene:
    name: 'PR Hygiene'
    needs: [changes]
    if: |
      needs.changes.outputs.has_product_changes == 'true' &&
      !contains(github.event.pull_request.labels.*.name, 'skip-hygiene')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 0

      - name: Require tests + changelog fragment for code changes
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail

          CHANGED=$(git diff --name-only "${BASE_SHA}...${HEAD_SHA}")
          echo "Changed files:"; echo "${CHANGED}" | sed 's/^/  /'

          # Product (non-test) source that ought to be covered by a test.
          CODE=$(echo "${CHANGED}" | grep -E \
            -e '^app/api/src/.*\.js$' \
            -e '^app/ui/src/.*\.(js|jsx)$' \
            -e '^tools/.*\.ps1$' \
            -e '^setup/docker/.*\.ps1$' \
            | grep -vE '\.(test|spec)\.(js|jsx)$' || true)

          # Anything that counts as a test.
          TESTS=$(echo "${CHANGED}" | grep -E \
            -e '\.(test|spec)\.(js|jsx)$' \
            -e '^app/ui/e2e/' \
            -e '^test/' \
            -e '\.Tests\.ps1$' \
            -e '/Test-[^/]+\.ps1$' \
            -e '/Start-Mock[^/]+\.ps1$' || true)

          # Changelog fragment (the project mandates one per branch).
          FRAGMENT=$(echo "${CHANGED}" | grep -E '^changes/.*\.md$' || true)

          FAIL=0
          if [ -n "${CODE}" ]; then
            if [ -z "${TESTS}" ]; then
              echo "::error::Source code changed but no test files were added or modified."
              echo "Add or extend a test: *.test.js (API), app/ui/e2e/*.spec.js (UI), *.Tests.ps1 (Pester unit), or Test-*.ps1 (crawler integration)."
              FAIL=1
            fi
            if [ -z "${FRAGMENT}" ]; then
              echo "::error::Code changed but no changelog fragment found. Add a bullet to changes/<branch>.md (never edit CHANGES.md)."
              FAIL=1
            fi
          else
            echo "No product code changed — tests/changelog not required."
          fi

          if [ "${FAIL}" = "1" ]; then
            echo "If this change genuinely needs neither, a maintainer can add the 'skip-hygiene' label."
            exit 1
          fi
          echo "PR hygiene OK."

  # ── Stage 8: Crawler manifest completeness ──────────────────────────────────
  # Every user-facing crawler must have a CrawlerMeta.js. Migrated crawlers must
  # not reappear as hardcoded strings in CrawlersPage.jsx. Library crawlers (used
  # only as dependsOn, e.g. odata) are exempt. Crawlers listed in PENDING are
  # allowed to be missing CrawlerMeta.js while they await migration.
  crawler-manifest:
    name: 'Lint: Crawler manifest completeness'
    needs: [changes]
    if: needs.changes.outputs.has_product_changes == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Check CrawlerMeta.js presence and no crawler-specific drift into app/ui
        run: |
          set -euo pipefail

          # Library crawlers (dependsOn only) — never need CrawlerMeta.js.
          LIBRARY_CRAWLERS="odata"

          # Crawlers pending migration to the manifest-based plugin system.
          # Remove a type from here once its CrawlerMeta.js is in place.
          PENDING_MIGRATION="entra-id demo custom-connector"

          FAIL=0

          for manifest in tools/crawlers/*/crawler.json; do
            dir=$(dirname "$manifest")
            type=$(node -e "process.stdout.write(require('./$manifest').type ?? '')")
            [ -z "$type" ] && continue

            # Skip library crawlers entirely.
            if echo "$LIBRARY_CRAWLERS" | grep -qw "$type"; then continue; fi

            # Check CrawlerMeta.js exists (skip if pending migration).
            meta="$dir/CrawlerMeta.js"
            if ! echo "$PENDING_MIGRATION" | grep -qw "$type"; then
              if [ ! -f "$meta" ]; then
                echo "::error file=$manifest::Crawler '$type' is missing CrawlerMeta.js — add one to $dir/ or add '$type' to PENDING_MIGRATION in pr.yml"
                FAIL=1
              fi
            fi

            # Migrated crawlers must not have crawler-specific files or
            # hardcoded type strings anywhere under app/ui/ — everything
            # about a migrated crawler belongs in tools/crawlers/<type>/.
            # (Pending-migration crawlers are still allowed to be there.)
            if ! echo "$PENDING_MIGRATION" | grep -qw "$type"; then
              stray_files=$(find app/ui/src app/ui/e2e -iname "*${type}*" 2>/dev/null || true)
              if [ -n "$stray_files" ]; then
                echo "::error::Found file(s) named after crawler type '$type' outside tools/crawlers/$type/ — move them there:"
                echo "$stray_files"
                FAIL=1
              fi

              hits=$(grep -rlE "(['\"])${type}\1" app/ui/src app/ui/e2e 2>/dev/null || true)
              if [ -n "$hits" ]; then
                echo "::error::Crawler type '$type' is hardcoded as a string outside tools/crawlers/$type/ — it must be discovered via CrawlerMeta.js only:"
                echo "$hits"
                FAIL=1
              fi
            fi
          done

          if [ "$FAIL" = "1" ]; then exit 1; fi
          echo "Crawler manifest check passed."

  # ── Stage 9: Crawler docs registered in site nav ─────────────────────────
  # Every page under docs/sync/ documents a data source / crawler and must be
  # reachable from the docs site menu. mkdocs.yml's nav: is hand-curated, so a
  # new page is easy to forget — this check fails the build instead of letting
  # it go unnoticed (it happened before, see PR #292).
  docs-nav:
    name: 'Lint: Crawler docs registered in site nav'
    needs: [changes]
    if: needs.changes.outputs.has_product_changes == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Check every docs/sync/*.md page is referenced in mkdocs.yml
        run: |
          set -euo pipefail
          FAIL=0
          for f in docs/sync/*.md; do
            rel="${f#docs/}"
            if ! grep -qF "$rel" mkdocs.yml; then
              echo "::error file=mkdocs.yml::docs/$rel is not referenced in mkdocs.yml — add it under nav: so it appears in the docs site menu"
              FAIL=1
            fi
          done
          if [ "$FAIL" = "1" ]; then exit 1; fi
          echo "All crawler docs are registered in the site nav."

  # ── Gate: single required check for branch protection ───────────────────
  # Replaces the individual job requirements in branch protection. Update branch
  # protection to require only "ci-passed" — not each individual job name.
  #
  # Migration: add ci-passed as a NEW branch protection requirement alongside
  # the old ones, verify on 2-3 PRs, then remove the old individual requirements.
  # Never remove + add in the same step (creates a window where PRs can merge unprotected).
  #
  # Passing on a housekeeping push (all jobs skipped) is INTENTIONAL.
  ci-passed:
    name: 'CI Passed'
    needs:
      - changes
      - filter
      - lint-ps
      - lint-js
      - unit-tests
      - unit-js
      - unit-ui
      - node-launcher-ui-build
      - openapi
      - audit
      - hygiene
      - crawler-manifest
      - docs-nav
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check results
        run: |
          FAILED=$(echo '${{ toJSON(needs) }}' \
            | jq -r 'to_entries | .[] | select(.value.result == "failure" or .value.result == "cancelled") | .key')
          if [ -n "$FAILED" ]; then
            echo "## CI Failed" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "The following jobs failed or were cancelled:" >> "$GITHUB_STEP_SUMMARY"
            echo "$FAILED" | while read -r job; do
              echo "- $job" >> "$GITHUB_STEP_SUMMARY"
            done
            exit 1
          fi
          echo "All checks passed or were legitimately skipped." >> "$GITHUB_STEP_SUMMARY"

  # ── PR Summary stub ─────────────────────────────────────────────────────────
  # The Claude GitHub App (issue triage, autofix, release-notes synthesis) creates
  # a check suite named "PR Summary" on every PR push. It stays queued if its
  # auto-summary feature doesn't fire, showing as "expected" in the GitHub UI.
  # This stub job emits a completed "PR Summary" check run that overshadows the
  # pending app suite — same check name, workflow wins in the UI display.
  pr-summary:
    name: 'PR Summary'
    needs: [ci-passed]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR Summary check satisfied by CI gate."

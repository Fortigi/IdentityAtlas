{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing for the optional-feature flag resolver. featureFlags.js is a small file that decides something consequential: whether a stored WorkerConfig override wins over an env-var default, per flag. A test that merely executed those lines would still pass if the precedence were inverted, so line coverage says nothing here — mutation does. Scoped to src/featureFlags.js and its OWN unit test (vitest.stryker.features.config.js), which is fully mocked and reads no real filesystem, so it runs clean inside Stryker's app/api-only sandbox. Run: cd app/api && npm run test:mutation:features. MUTATION FLOOR: `break` is set a few points under the measured score and is a RATCHET — it only goes up. Enforced by the weekly .github/workflows/js-mutation.yml, never on a PR. Raise it when the score rises; never lower it to make a red run green — a drop means a test stopped discriminating. Measured 100.0% when the floor was set to 98.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.stryker.features.config.js"
  },
  "reporters": [
    "clear-text",
    "json"
  ],
  "jsonReporter": {
    "fileName": "reports/stryker-features.json"
  },
  "mutate": [
    "src/featureFlags.js"
  ],
  "mutationExclusions": {
    "src/ingest/systemBoundary.js": "Not yet measured. Added by SEC-2026-09 (per-system ingest boundary). Stryker could not produce an honest score on the workstation this was written on: its vitest runner does not activate mutants there (this config’s own src/featureFlags.js scored 18.5% locally against the 100% measured in CI), so any number quoted here would be wrong. The decisions in this file are pinned by its own fully-mocked unit test with discriminating inputs; the ingest boundary it serves is also exercised end to end against a real schema by contract-tests/ingestSystemBoundary.integration.test.js. Measure it on Linux and move it into a scope’s mutate list.",
    "src/routes/ingest/dataPlane.js": "Not yet measured. Added by SEC-2026-09 (per-system ingest boundary). Stryker could not produce an honest score on the workstation this was written on: its vitest runner does not activate mutants there (this config’s own src/featureFlags.js scored 18.5% locally against the 100% measured in CI), so any number quoted here would be wrong. The decisions in this file are pinned by its own fully-mocked unit test with discriminating inputs; the ingest boundary it serves is also exercised end to end against a real schema by contract-tests/ingestSystemBoundary.integration.test.js. Measure it on Linux and move it into a scope’s mutate list.",
    "src/lib/serializedRunner.js": "Not yet measured. Added by SEC-2026-09 (per-system ingest boundary). Stryker could not produce an honest score on the workstation this was written on: its vitest runner does not activate mutants there (this config’s own src/featureFlags.js scored 18.5% locally against the 100% measured in CI), so any number quoted here would be wrong. The decisions in this file are pinned by its own fully-mocked unit test with discriminating inputs; the ingest boundary it serves is also exercised end to end against a real schema by contract-tests/ingestSystemBoundary.integration.test.js. Measure it on Linux and move it into a scope’s mutate list.",
    "src/db/connectionDefaults.js": "Not yet measured. Added by SEC-2026-09 (per-system ingest boundary). Stryker could not produce an honest score on the workstation this was written on: its vitest runner does not activate mutants there (this config’s own src/featureFlags.js scored 18.5% locally against the 100% measured in CI), so any number quoted here would be wrong. The decisions in this file are pinned by its own fully-mocked unit test with discriminating inputs; the ingest boundary it serves is also exercised end to end against a real schema by contract-tests/ingestSystemBoundary.integration.test.js. Measure it on Linux and move it into a scope’s mutate list.",
    "src/ingest/reconcileStale.js": "Not yet measured, for the same reason as the SEC-2026-09 ingest-boundary files above: Stryker cannot produce an honest score on the Windows workstation this was written on (its vitest runner does not activate mutants there), so any number quoted here would be fiction. The decisions in this file are pinned by src/ingest/reconcileStale.test.js with discriminating inputs -- the request parser is asserted against every rejection it can make (unknown entity, each of the five entities that carry no system scope, a non-integer or non-positive systemId, an unparseable or future timestamp), and the SQL builder is asserted on the soft-delete vs hard-delete split, the deletedAt IS NULL guard, the scope filter and the parameter ordering. The route wiring is covered in src/routes/ingest.coverage.test.js and the crawler side of the protocol (reconcile only after a completed run that fed the system) in test/unit/CSVCrawlerPhases.Tests.ps1. Measure it on Linux and move it into a scope mutate list."
  },
  "thresholds": {
    "high": 90,
    "low": 70,
    "break": 98
  },
  "concurrency": 4,
  "timeoutMS": 60000,
  "disableTypeChecks": false
}

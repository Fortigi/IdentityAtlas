// Teams bot (POC) — per-conversation state.
//
// Moved to nlreports/state.js together with the follow-up logic; the web route
// remembers what an answer was about the same way the bot does.

export * from '../nlreports/state.js';

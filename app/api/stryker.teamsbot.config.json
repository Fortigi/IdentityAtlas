{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing for the Teams bot: caller resolution, the caller-substitution pass, the card renderer, the conversation log, the one round of clarification state, language detection, the turn orchestration, and the bot's own authentication. WHY THESE FILES: every failure in this feature is silent and points the wrong way. Caller resolution answering for the wrong person, a substitution that leaves `@me` in a definition (so the report matches nothing and reads as 'you have no direct reports'), a row cap off by one, a scope caveat that stops appearing on a directory-wide answer to a question that said 'my', a permission check that accepts the wrong permission, a pending clarification that is answered twice -- each produces a perfectly formatted card that a manager will believe. There is no scope filter behind any of this in v1 (see teamsbot/caller.js), so the card IS the control. EXCLUDED, with reasons below: the two files that are Teams glue rather than logic. MUTATOR SET: StringLiteral and ObjectLiteral disabled, matching the sibling API configs -- so the reply wording in text.js and the SQL strings are not mutated; what text.js is actually measured on is detectLanguage and the key-parity check in text.test.js. Run: cd app/api && npm run test:mutation:teamsbot NOT YET MEASURED: this config ships with the feature; `break` is deliberately absent until a first run measures a score (a floor invented without a measurement is either slack or a red run on day one). Add `{ pkg: api, name: teamsbot, label: 'API Teams bot' }` to .github/workflows/js-mutation.yml once it is measured, and set the floor a few points under what it reports.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.stryker.teamsbot.config.js"
  },
  "reporters": [
    "clear-text",
    "json"
  ],
  "jsonReporter": {
    "fileName": "reports/stryker-teamsbot.json"
  },
  "mutate": [
    "src/teamsbot/auth.js",
    "src/teamsbot/caller.js",
    "src/teamsbot/callerSpec.js",
    "src/teamsbot/card.js",
    "src/teamsbot/log.js",
    "src/teamsbot/service.js",
    "src/teamsbot/state.js",
    "src/teamsbot/text.js"
  ],
  "mutationExclusions": {
    "src/teamsbot/handler.js": "Teams glue, not logic: it subscribes to the SDK's onMessage/onMembersAdded hooks, re-sends a typing indicator on an interval, and puts an attachment on the wire. What it decides is decided elsewhere and mutation-tested there -- who is asking (auth.js), what the answer is (service.js), what it looks like (card.js). The only branches it owns are the three no-caller replies, and separating them needs a live TurnContext with a token service behind it, which is what the Bot Framework's own test adapter is for and what src/routes/teamsBot.test.js covers at the HTTP boundary instead.",
    "src/routes/teamsBot.js": "Wiring, not logic: it constructs the CloudAdapter from environment variables, memoises it, and hands the request to it. Every rule it applies is a middleware declared one line at a time -- requirePermission, requireFeature, and their ORDER -- and the order is what actually matters here (permission first, so a disabled bot cannot be discovered by a caller without permission). That is asserted directly, against a real Express app with auth enabled, in src/routes/teamsBot.test.js; a mutant that swapped the two would be caught there rather than by mutating the adapter construction around them."
  },
  "thresholds": {
    "high": 90,
    "low": 70
  },
  "concurrency": 2,
  "timeoutMS": 60000,
  "disableTypeChecks": false,
  "mutator": {
    "excludedMutations": [
      "StringLiteral",
      "ObjectLiteral"
    ]
  }
}

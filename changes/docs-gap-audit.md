- Added a documentation gap audit (`docs/docs-gap-audit.md`) mapping shipped features to their documentation coverage, as the basis for a remediation plan.
- Added a living documentation remediation plan (`docs/docs-remediation-plan.md`) that reviews features cluster-by-cluster against each documentation type (analyst/user / operator / contributor / API-consumer) and validates usability through end-to-end journey walkthroughs.
- Corrected the Ingest API reference: the success response now documents the real fields (`table`, `inserted`, `updated`, `deleted`, `records`, `durationMs`, optional `systemIds`) and `201 Created` status, and the validation tables now list the actually-accepted `assignmentType` (`Direct`/`Indirect`/`Eligible`) and `relationshipType` values.
- Added a Custom Connector guide (`docs/sync/custom-connector.md`) for pushing data into Identity Atlas over the Ingest API, with a copy-paste ingest example, the real response body, and self-service key rotation.
- Documented the authoritative external API base URL (resolve `/api` against `PUBLIC_BASE_URL`) in the API reference overview.
- Corrected the Matrix badge-collapse reference: the matrix shows only Direct / Indirect / Eligible — the retired `Owner` and `Governed` badges are gone (ownership is now its own resource, governed access is a flag), and the source-detail types (`DirectoryRole`, `DirectoryRoleEligible`, `OAuth2Grant`, `AppRole`, `AppRoleViaGroup`) are shown collapsing to their displayed badge.
- Fixed stale data-source references in the Matrix and Entity Detail API docs to name the real `vw_ResourceUserPermissionAssignments` view (the old `mat_UserPermissionAssignments` object does not exist in the current schema).
- Documented the Effective Access API endpoints (resolve who can reach a resource and how, including inherited access and truncation reporting).
- Added an analyst task guide, "Answer: who can access resource X, and why" (`docs/ui/effective-access.md`), covering direct vs. inherited holders and how to read the access path.
- Added an analyst guide for the Contexts screen (`docs/ui/contexts.md`): variants and target types, the two-pane tree/list layout, creating a context via the New-Context wizard, running/syncing plugins, drag-to-reparent, and filtering the Matrix by a context.
- Updated the Context Redesign architecture docs to mark the feature as shipped in v6 (was "Design proposal — not yet implemented") and corrected the frontend-structure section to reflect the files that actually shipped.
- Added an operator guide for the Admin → Data tab (`docs/admin/data-tab.md`) covering curated tag/category export & import, the Excel Power Query workbook, Deleted Data & History Retention (180-day default, 0 disables), and the Danger Zone "Clean Database" wipe (what it removes vs. keeps, plus its confirmations and rate limit).
- Added a step-by-step guide to setting up authentication and roles (`docs/admin/authentication.md`): registering the app, assigning the first admin before enabling auth, editing the role → permission mapping, and recovering from a lockout.
- Added a Dashboard user guide (`docs/ui/dashboard.md`) explaining the entity-count cards, feature status row, brain graph, last-sync and sync-log links, and the "Configure a crawler" onboarding flow.
- Corrected the Audit History doc to point at the History Retention section under Admin → Data (there is no separate "Admin > History Retention" tab).
- Added a link to the Entra ID sync guide from the Quick Start "What's Next" section.
- Made the Quick Start and home-page install instructions consistent: both now spell out that a `POSTGRES_PASSWORD` must be set in `.env` before the production stack will start (copying `.env.example` alone leaves it blank).
- Corrected the documented risk-tier thresholds to match the app (Critical 90–100, High 70–89, Medium 40–69) and documented the analyst-override limits (whole number −50 to +50, 3–500 character justification).
- Corrected the Governance docs: governed access is the `governed=true` flag (not a `Governed` assignment type), fixed the `GovernanceCategories` table names, documented the governance summary and review-compliance endpoints, and clarified that Identity Atlas reports on but does not conduct certifications (decisions are made in the source IGA and mirrored read-only).
- Rewrote the Database Views reference so every example query uses the real view columns, and corrected the view status (the permission-matrix views are materialized and need `REFRESH`, not "planned for a future release").
- Marked the assignment-model redesign as implemented (shipped in migrations 044–049).
- Expanded the Entra ID sync guide: documented the app / service-principal / directory-role phase toggles with guidance on which to enable, surfaced the required Graph permissions in the setup section, and fixed the "What Gets Synced" diagram (ownership is its own resource, not an `Owner` assignment type).
- Fixed crawler-authoring docs: correct `getConfigSecret` signature, the `@ui/` wizard import alias, and a minimal example that dot-sources the shared ingest helpers.
- Refreshed the Role Mining UI guide for v6: the Dashboard landing page, the Contexts tab, the Matrix Filter Wizard / saved matrices / roll-up-by-attribute (replacing the removed user-limit slider and the retired Org Chart), all ten Admin sub-tabs, tags-as-contexts, and read-only certifications.

- Internal maintainability: reduced cognitive complexity across API modules by extracting focused, unit-tested helpers — no functional change. Files brought under the complexity gate (removed from the grandfathered baseline) so far: the performance-metrics summary, ingest session finalize, crawler-auth middleware, the principal-type context plugin, ingest normalization, and the crawler scheduler.

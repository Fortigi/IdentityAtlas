# DoR deploy smoke test

Throwaway PR to exercise the `dor-deploy` workflow (build-side slice A):
label this PR `deploy-to-sidekick` → a dor-build runner should deploy this
branch to a sidekick and comment its N.build.identityatlas.io URL.

Safe to close — not a real change.
